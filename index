import React, { useState, useMemo, useEffect } from 'react';
import { 
  GraduationCap, BookOpen, AlertCircle, Award, CheckCircle2, 
  TrendingUp, Plus, Trash2, Edit2, Moon, Sun, Palette,
  User, Save, X, ChevronDown, ChevronUp, BarChart
} from 'lucide-react';

// --- Constants & Configuration ---
const GRADES = {
  'A': 4.0, 'B+': 3.5, 'B': 3.0, 'C+': 2.5, 'C': 2.0, 'D+': 1.5, 'D': 1.0, 'F': 0.0, 'W': null
};

const THEMES = [
  { id: 'slate', name: 'Slate', primary: 'bg-slate-800', text: 'text-slate-800', lightPrimary: 'bg-slate-100' },
  { id: 'emerald', name: 'Emerald', primary: 'bg-emerald-600', text: 'text-emerald-600', lightPrimary: 'bg-emerald-50' },
  { id: 'indigo', name: 'Indigo', primary: 'bg-indigo-600', text: 'text-indigo-600', lightPrimary: 'bg-indigo-50' },
  { id: 'rose', name: 'Rose', primary: 'bg-rose-600', text: 'text-rose-600', lightPrimary: 'bg-rose-50' },
  { id: 'amber', name: 'Amber', primary: 'bg-amber-600', text: 'text-amber-600', lightPrimary: 'bg-amber-50' },
];

const CURRICULUM = {
  genEd: {
    title: 'หมวดศึกษาทั่วไป',
    req: 33,
    sub: {
      'GE_TH': { title: 'ภาษาไทย', req: 3 },
      'GE_EN': { title: 'ภาษาต่างประเทศ', req: 6 },
      'GE_SCI': { title: 'วิทยาศาสตร์', req: 3 },
      'GE_MATH': { title: 'คณิตศาสตร์', req: 3 },
      'GE_SOC': { title: 'สังคมศาสตร์', req: 3 },
      'GE_HUM': { title: 'มนุษยศาสตร์', req: 3 },
      'GE_ELEC': { title: 'เลือกศึกษาทั่วไป', req: 12 },
    }
  },
  core: { title: 'กลุ่มวิชาแกน', req: 27 },
  specific: {
    title: 'กลุ่มวิชาเฉพาะ',
    req: 67,
    sub: {
      'SPEC_COMP': { title: 'วิชาชีพบังคับ', req: 52 },
      'SPEC_ELEC': { title: 'วิชาชีพเลือก', req: 15 },
    }
  },
  free: { title: 'หมวดเลือกเสรี', req: 6 }
};

const CATEGORY_OPTIONS = [
  { value: 'GE_TH', label: 'ศึกษาทั่วไป - ภาษาไทย' },
  { value: 'GE_EN', label: 'ศึกษาทั่วไป - ภาษาต่างประเทศ' },
  { value: 'GE_SCI', label: 'ศึกษาทั่วไป - วิทยาศาสตร์' },
  { value: 'GE_MATH', label: 'ศึกษาทั่วไป - คณิตศาสตร์' },
  { value: 'GE_SOC', label: 'ศึกษาทั่วไป - สังคมศาสตร์' },
  { value: 'GE_HUM', label: 'ศึกษาทั่วไป - มนุษยศาสตร์' },
  { value: 'GE_ELEC', label: 'ศึกษาทั่วไป - เลือกศึกษาทั่วไป' },
  { value: 'CORE', label: 'วิชาแกน' },
  { value: 'SPEC_COMP', label: 'วิชาเฉพาะ - วิชาชีพบังคับ' },
  { value: 'SPEC_ELEC', label: 'วิชาเฉพาะ - วิชาชีพเลือก' },
  { value: 'FREE', label: 'เลือกเสรี' },
];

const INITIAL_COURSES = [
  { id: '1', term: '1/2566', code: 'TH101', name: 'การใช้ภาษาไทย', credits: 3, grade: 'A', category: 'GE_TH' },
  { id: '2', term: '1/2566', code: 'EN101', name: 'ภาษาอังกฤษพื้นฐาน 1', credits: 3, grade: 'B+', category: 'GE_EN' },
  { id: '3', term: '1/2566', code: 'SC101', name: 'วิทยาศาสตร์ในชีวิตประจำวัน', credits: 3, grade: 'A', category: 'GE_SCI' },
  { id: '4', term: '1/2566', code: 'CS101', name: 'การเขียนโปรแกรม 1', credits: 3, grade: 'B', category: 'CORE' },
  { id: '5', term: '2/2566', code: 'EN102', name: 'ภาษาอังกฤษพื้นฐาน 2', credits: 3, grade: 'A', category: 'GE_EN' },
  { id: '6', term: '2/2566', code: 'MA101', name: 'คณิตศาสตร์เบื้องต้น', credits: 3, grade: 'C+', category: 'GE_MATH' },
  { id: '7', term: '2/2566', code: 'CS102', name: 'การเขียนโปรแกรม 2', credits: 3, grade: 'A', category: 'CORE' },
  { id: '8', term: '2/2566', code: 'SE201', name: 'วิศวกรรมซอฟต์แวร์', credits: 3, grade: 'A', category: 'SPEC_COMP' },
];

const MOCK_USER = {
  name: 'สมชาย',
  surname: 'เรียนดี',
  studentId: '661000001'
};

export default function App() {
  // State
  const [isDarkMode, setIsDarkMode] = useState(false);
  const [activeTheme, setActiveTheme] = useState('indigo');
  const [user, setUser] = useState(MOCK_USER);
  const [isEditingUser, setIsEditingUser] = useState(false);
  const [courses, setCourses] = useState(INITIAL_COURSES);
  const [showAddCourse, setShowAddCourse] = useState(false);
  
  // Theme styling helpers
  const themeObj = THEMES.find(t => t.id === activeTheme) || THEMES[0];
  const primaryBg = themeObj.primary;
  const primaryText = themeObj.text;
  const lightBg = themeObj.lightPrimary;

  // Helpers
  const calcGPA = (courseList) => {
    let totalPoints = 0;
    let totalCredits = 0;
    courseList.forEach(c => {
      if (GRADES[c.grade] !== null && GRADES[c.grade] !== undefined) {
        totalPoints += GRADES[c.grade] * c.credits;
        totalCredits += c.credits;
      }
    });
    return totalCredits === 0 ? 0 : (totalPoints / totalCredits);
  };

  const getCredits = (courseList) => {
    return courseList.reduce((acc, c) => acc + (GRADES[c.grade] !== null ? c.credits : 0), 0);
  };

  // Derived Data
  const gpax = calcGPA(courses);
  const totalCreditsEarned = getCredits(courses);
  const totalCreditsReq = 133;

  // Academic Status
  const studentStatus = useMemo(() => {
    if (totalCreditsEarned < 15) return { label: 'ปกติ', color: 'text-emerald-500' }; // Grace period for freshies
    if (gpax < 1.50) return { label: 'พ้นสภาพ (Retire)', color: 'text-rose-500' };
    if (gpax < 2.00) return { label: 'วิทยาทัณฑ์ (Probation)', color: 'text-amber-500' };
    return { label: 'ปกติ', color: 'text-emerald-500' };
  }, [gpax, totalCreditsEarned]);

  // Honors Prediction
  const honorsPrediction = useMemo(() => {
    const hasF = courses.some(c => c.grade === 'F');
    // Simple check for retakes: duplicate course codes
    const codes = courses.map(c => c.code);
    const hasRetake = codes.length !== new Set(codes).size;

    if (hasF || hasRetake) return { label: 'หมดสิทธิ์ (F/รีเกรด)', color: 'text-slate-400' };
    if (gpax >= 3.75) return { label: 'เกียรตินิยมอันดับ 1', color: 'text-amber-500' };
    if (gpax >= 3.25) return { label: 'เกียรตินิยมอันดับ 2', color: 'text-indigo-500' };
    return { label: 'ไม่มี', color: 'text-slate-400' };
  }, [gpax, courses]);

  // Group Courses by Term
  const coursesByTerm = useMemo(() => {
    const groups = {};
    courses.forEach(c => {
      if (!groups[c.term]) groups[c.term] = [];
      groups[c.term].push(c);
    });
    // Sort terms
    return Object.keys(groups).sort((a, b) => {
      const [termA, yearA] = a.split('/');
      const [termB, yearB] = b.split('/');
      if (yearA !== yearB) return parseInt(yearA) - parseInt(yearB);
      return parseInt(termA) - parseInt(termB);
    }).map(term => ({
      term,
      courses: groups[term],
      gpa: calcGPA(groups[term]),
      credits: getCredits(groups[term])
    }));
  }, [courses]);

  // Progress Calculation
  const getProgress = (categoryKeys, requiredCredits) => {
    const keys = Array.isArray(categoryKeys) ? categoryKeys : [categoryKeys];
    const earned = courses
      .filter(c => keys.includes(c.category) && c.grade !== 'F' && c.grade !== 'W')
      .reduce((acc, c) => acc + c.credits, 0);
    return { earned, req: requiredCredits, complete: earned >= requiredCredits };
  };

  // Trend Chart Logic
  const TrendChart = () => {
    if (coursesByTerm.length === 0) return <div className="text-sm text-gray-400">ไม่มีข้อมูลเกรด</div>;
    const maxVal = 4.0;
    const height = 120;
    const width = 100; // percentage
    const points = coursesByTerm.map((t, i) => {
      const x = (i / (coursesByTerm.length > 1 ? coursesByTerm.length - 1 : 1)) * 100;
      const y = height - (t.gpa / maxVal) * height;
      return `${x},${y}`;
    }).join(' ');

    return (
      <div className="relative w-full h-[140px] mt-4 overflow-visible">
        <svg viewBox={`0 -10 100 ${height + 20}`} preserveAspectRatio="none" className="w-full h-full overflow-visible">
          <polyline
            points={points}
            fill="none"
            stroke={themeObj.id === 'slate' ? '#475569' : `var(--primary-color, #4f46e5)`}
            strokeWidth="2"
            strokeLinecap="round"
            strokeLinejoin="round"
            className={primaryText}
          />
          {coursesByTerm.map((t, i) => {
            const x = (i / (coursesByTerm.length > 1 ? coursesByTerm.length - 1 : 1)) * 100;
            const y = height - (t.gpa / maxVal) * height;
            return (
              <g key={t.term}>
                <circle cx={x} cy={y} r="3" fill="currentColor" className={`text-white dark:text-slate-800 ${primaryText} stroke-current stroke-2`} />
                <text x={x} y={y - 10} fontSize="5" textAnchor="middle" fill="currentColor" className="text-slate-500 dark:text-slate-400 font-medium">
                  {t.gpa.toFixed(2)}
                </text>
                <text x={x} y={height + 15} fontSize="4" textAnchor="middle" fill="currentColor" className="text-slate-400 dark:text-slate-500">
                  {t.term}
                </text>
              </g>
            );
          })}
        </svg>
      </div>
    );
  };

  // Add/Edit Course Form
  const CourseForm = ({ initialData, onSave, onCancel }) => {
    const [formData, setFormData] = useState(initialData || {
      term: '1/2567', code: '', name: '', credits: 3, grade: 'A', category: 'GE_TH'
    });

    const handleSubmit = (e) => {
      e.preventDefault();
      onSave({ ...formData, id: formData.id || Date.now().toString(), credits: Number(formData.credits) });
    };

    return (
      <form onSubmit={handleSubmit} className={`p-4 rounded-xl border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800/50 space-y-4 mb-4`}>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-6 gap-3">
          <div className="lg:col-span-1">
            <label className="block text-xs font-medium text-slate-500 dark:text-slate-400 mb-1">เทอม (เช่น 1/2567)</label>
            <input required type="text" value={formData.term} onChange={e => setFormData({...formData, term: e.target.value})} className="w-full text-sm p-2 rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-700 text-slate-800 dark:text-slate-200 focus:ring-2 focus:ring-indigo-500 outline-none transition-all" />
          </div>
          <div className="lg:col-span-1">
            <label className="block text-xs font-medium text-slate-500 dark:text-slate-400 mb-1">รหัสวิชา</label>
            <input required type="text" value={formData.code} onChange={e => setFormData({...formData, code: e.target.value})} className="w-full text-sm p-2 rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-700 text-slate-800 dark:text-slate-200 focus:ring-2 focus:ring-indigo-500 outline-none transition-all" />
          </div>
          <div className="lg:col-span-2">
            <label className="block text-xs font-medium text-slate-500 dark:text-slate-400 mb-1">ชื่อวิชา</label>
            <input required type="text" value={formData.name} onChange={e => setFormData({...formData, name: e.target.value})} className="w-full text-sm p-2 rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-700 text-slate-800 dark:text-slate-200 focus:ring-2 focus:ring-indigo-500 outline-none transition-all" />
          </div>
          <div className="lg:col-span-1">
            <label className="block text-xs font-medium text-slate-500 dark:text-slate-400 mb-1">หน่วยกิต</label>
            <input required type="number" min="0" max="10" value={formData.credits} onChange={e => setFormData({...formData, credits: e.target.value})} className="w-full text-sm p-2 rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-700 text-slate-800 dark:text-slate-200 focus:ring-2 focus:ring-indigo-500 outline-none transition-all" />
          </div>
          <div className="lg:col-span-1">
            <label className="block text-xs font-medium text-slate-500 dark:text-slate-400 mb-1">เกรด</label>
            <select value={formData.grade} onChange={e => setFormData({...formData, grade: e.target.value})} className="w-full text-sm p-2 rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-700 text-slate-800 dark:text-slate-200 focus:ring-2 focus:ring-indigo-500 outline-none transition-all">
              {Object.keys(GRADES).map(g => <option key={g} value={g}>{g}</option>)}
            </select>
          </div>
          <div className="lg:col-span-6">
            <label className="block text-xs font-medium text-slate-500 dark:text-slate-400 mb-1">หมวดหมู่</label>
            <select value={formData.category} onChange={e => setFormData({...formData, category: e.target.value})} className="w-full text-sm p-2 rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-700 text-slate-800 dark:text-slate-200 focus:ring-2 focus:ring-indigo-500 outline-none transition-all">
              {CATEGORY_OPTIONS.map(c => <option key={c.value} value={c.value}>{c.label}</option>)}
            </select>
          </div>
        </div>
        <div className="flex justify-end gap-2 mt-4">
          <button type="button" onClick={onCancel} className="px-4 py-2 text-sm rounded-lg border border-slate-300 dark:border-slate-600 text-slate-600 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-700 transition-colors">ยกเลิก</button>
          <button type="submit" className={`px-4 py-2 text-sm rounded-lg text-white ${primaryBg} hover:opacity-90 transition-opacity flex items-center gap-2`}>
            <Save size={16} /> บันทึก
          </button>
        </div>
      </form>
    );
  };

  useEffect(() => {
    if (isDarkMode) {
      document.documentElement.classList.add('dark');
    } else {
      document.documentElement.classList.remove('dark');
    }
  }, [isDarkMode]);

  // Inject styles for fonts
  useEffect(() => {
    const style = document.createElement('style');
    style.innerHTML = `
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
      @import url('https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500;600&display=swap');
      
      body {
        font-family: 'Inter', 'Kanit', sans-serif;
        background-color: ${isDarkMode ? '#0f172a' : '#f8fafc'};
        color: ${isDarkMode ? '#f1f5f9' : '#0f172a'};
        transition: background-color 0.3s ease, color 0.3s ease;
      }
    `;
    document.head.appendChild(style);
    return () => document.head.removeChild(style);
  }, [isDarkMode]);

  return (
    <div className={`min-h-screen ${isDarkMode ? 'dark' : ''} bg-slate-50 dark:bg-slate-900 transition-colors duration-300`}>
      {/* Navbar / Header */}
      <nav className="sticky top-0 z-50 bg-white/80 dark:bg-slate-800/80 backdrop-blur-md border-b border-slate-200 dark:border-slate-700/50">
        <div className="max-w-6xl mx-auto px-4 h-16 flex items-center justify-between">
          <div className="flex items-center gap-3">
            <div className={`w-10 h-10 rounded-xl ${primaryBg} text-white flex items-center justify-center shadow-lg shadow-${themeObj.id}-500/30`}>
              <GraduationCap size={24} />
            </div>
            <h1 className="text-xl font-bold tracking-tight text-slate-800 dark:text-white">GradWay<span className={primaryText}>.</span></h1>
          </div>

          <div className="flex items-center gap-4">
            {/* Theme Selector */}
            <div className="hidden sm:flex items-center gap-2 bg-slate-100 dark:bg-slate-700/50 p-1 rounded-full">
              {THEMES.map(t => (
                <button
                  key={t.id}
                  onClick={() => setActiveTheme(t.id)}
                  className={`w-6 h-6 rounded-full transition-all ${t.id === activeTheme ? 'ring-2 ring-offset-2 ring-slate-400 dark:ring-slate-500 scale-110' : 'opacity-70 hover:opacity-100'}`}
                  style={{ backgroundColor: t.id === 'slate' ? '#475569' : t.id === 'emerald' ? '#10b981' : t.id === 'indigo' ? '#6366f1' : t.id === 'rose' ? '#f43f5e' : '#f59e0b' }}
                  title={t.name}
                />
              ))}
            </div>

            {/* Dark Mode Toggle */}
            <button 
              onClick={() => setIsDarkMode(!isDarkMode)}
              className="p-2 rounded-full text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-700 transition-colors"
            >
              {isDarkMode ? <Sun size={20} /> : <Moon size={20} />}
            </button>
          </div>
        </div>
      </nav>

      <main className="max-w-6xl mx-auto px-4 py-8 space-y-8">
        
        {/* User Profile Banner */}
        <section className="bg-white dark:bg-slate-800 rounded-3xl p-6 md:p-8 shadow-sm border border-slate-100 dark:border-slate-700/50 flex flex-col md:flex-row justify-between items-start md:items-center gap-6 relative overflow-hidden">
          <div className={`absolute right-0 top-0 w-64 h-64 bg-gradient-to-br from-transparent to-${themeObj.id}-500/10 rounded-full blur-3xl -mr-20 -mt-20 pointer-events-none`}></div>
          <div className="flex items-center gap-6">
            <div className="w-20 h-20 rounded-2xl bg-slate-100 dark:bg-slate-700 flex items-center justify-center text-slate-400">
              <User size={40} />
            </div>
            {isEditingUser ? (
              <div className="flex flex-col gap-2">
                <input type="text" value={user.studentId} onChange={e => setUser({...user, studentId: e.target.value})} className="px-3 py-1 text-sm rounded border dark:bg-slate-700 dark:border-slate-600 dark:text-white" placeholder="รหัสนักศึกษา" />
                <div className="flex gap-2">
                  <input type="text" value={user.name} onChange={e => setUser({...user, name: e.target.value})} className="px-3 py-1 text-sm rounded border dark:bg-slate-700 dark:border-slate-600 dark:text-white" placeholder="ชื่อ" />
                  <input type="text" value={user.surname} onChange={e => setUser({...user, surname: e.target.value})} className="px-3 py-1 text-sm rounded border dark:bg-slate-700 dark:border-slate-600 dark:text-white" placeholder="นามสกุล" />
                </div>
                <button onClick={() => setIsEditingUser(false)} className={`text-sm mt-2 w-fit px-4 py-1 rounded-full ${primaryBg} text-white`}>บันทึก</button>
              </div>
            ) : (
              <div>
                <p className="text-sm font-medium text-slate-500 dark:text-slate-400 mb-1">รหัสนักศึกษา {user.studentId}</p>
                <h2 className="text-2xl font-bold text-slate-800 dark:text-white flex items-center gap-2">
                  {user.name} {user.surname}
                  <button onClick={() => setIsEditingUser(true)} className="text-slate-400 hover:text-slate-600 dark:hover:text-slate-300"><Edit2 size={16} /></button>
                </h2>
                <div className="mt-2 flex gap-3">
                  <span className={`inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs font-medium ${studentStatus.label === 'ปกติ' ? 'bg-emerald-100 text-emerald-700 dark:bg-emerald-500/20 dark:text-emerald-400' : 'bg-rose-100 text-rose-700 dark:bg-rose-500/20 dark:text-rose-400'}`}>
                    <AlertCircle size={14} /> สถานภาพ: {studentStatus.label}
                  </span>
                </div>
              </div>
            )}
          </div>
          
          <div className="w-full md:w-auto grid grid-cols-2 gap-4">
            <div className="text-center p-4 rounded-2xl bg-slate-50 dark:bg-slate-700/50 border border-slate-100 dark:border-slate-600/50">
              <p className="text-xs font-medium text-slate-500 dark:text-slate-400 mb-1">เกรดเฉลี่ยสะสม (GPAX)</p>
              <p className={`text-3xl font-bold ${primaryText}`}>{gpax.toFixed(2)}</p>
            </div>
            <div className="text-center p-4 rounded-2xl bg-slate-50 dark:bg-slate-700/50 border border-slate-100 dark:border-slate-600/50">
              <p className="text-xs font-medium text-slate-500 dark:text-slate-400 mb-1">หน่วยกิตสะสม</p>
              <p className="text-3xl font-bold text-slate-800 dark:text-white">{totalCreditsEarned}<span className="text-base text-slate-400 font-normal">/{totalCreditsReq}</span></p>
            </div>
          </div>
        </section>

        {}
        {/* KPI Dashboard */}
        <section className="grid grid-cols-1 md:grid-cols-3 gap-6">
          <div className="bg-white dark:bg-slate-800 rounded-3xl p-6 shadow-sm border border-slate-100 dark:border-slate-700/50">
            <div className="flex items-center justify-between mb-4">
              <h3 className="font-semibold text-slate-800 dark:text-white flex items-center gap-2">
                <TrendingUp size={18} className={primaryText} /> แนวโน้มการศึกษา
              </h3>
            </div>
            <TrendChart />
          </div>

          <div className="bg-white dark:bg-slate-800 rounded-3xl p-6 shadow-sm border border-slate-100 dark:border-slate-700/50">
            <div className="flex items-center justify-between mb-2">
              <h3 className="font-semibold text-slate-800 dark:text-white flex items-center gap-2">
                <Award size={18} className="text-amber-500" /> การคาดการณ์เกียรตินิยม
              </h3>
            </div>
            <p className="text-sm text-slate-500 dark:text-slate-400 mb-4">อ้างอิงจากผลการเรียนปัจจุบัน</p>
            <div className={`p-4 rounded-2xl flex items-center justify-center text-lg font-bold border ${honorsPrediction.label === 'เกียรตินิยมอันดับ 1' ? 'bg-amber-50 border-amber-200 text-amber-600 dark:bg-amber-500/10 dark:border-amber-500/30' : honorsPrediction.label === 'เกียรตินิยมอันดับ 2' ? 'bg-indigo-50 border-indigo-200 text-indigo-600 dark:bg-indigo-500/10 dark:border-indigo-500/30' : 'bg-slate-50 border-slate-200 text-slate-500 dark:bg-slate-700/50 dark:border-slate-600'}`}>
              {honorsPrediction.label}
            </div>
            <div className="mt-4 space-y-2 text-xs text-slate-500 dark:text-slate-400">
              <p className="flex justify-between"><span>GPA สะสม {'>='} 3.75</span> <span>อันดับ 1</span></p>
              <p className="flex justify-between"><span>GPA สะสม {'>='} 3.25</span> <span>อันดับ 2</span></p>
              <p className="text-rose-500 dark:text-rose-400 mt-2">*เงื่อนไข: ไม่เคยติด F, ไม่เคยรีเกรด</p>
            </div>
          </div>

          <div className="bg-white dark:bg-slate-800 rounded-3xl p-6 shadow-sm border border-slate-100 dark:border-slate-700/50 flex flex-col justify-between">
            <div>
              <div className="flex items-center justify-between mb-2">
                <h3 className="font-semibold text-slate-800 dark:text-white flex items-center gap-2">
                  <CheckCircle2 size={18} className="text-emerald-500" /> ความคืบหน้าหลักสูตร
                </h3>
              </div>
              <p className="text-sm text-slate-500 dark:text-slate-400 mb-6">ภาพรวมหน่วยกิตที่เรียนผ่านแล้ว</p>
            </div>
            
            <div className="relative pt-6">
              <div className="flex justify-between items-end mb-2">
                <span className="text-3xl font-bold text-slate-800 dark:text-white">{Math.min(100, Math.round((totalCreditsEarned/totalCreditsReq)*100))}%</span>
                <span className="text-sm font-medium text-slate-500 dark:text-slate-400">สำเร็จแล้ว</span>
              </div>
              <div className="w-full bg-slate-100 dark:bg-slate-700 rounded-full h-3 overflow-hidden">
                <div className={`h-3 rounded-full ${primaryBg} transition-all duration-1000`} style={{ width: `${Math.min(100, (totalCreditsEarned/totalCreditsReq)*100)}%` }}></div>
              </div>
            </div>
          </div>
        </section>

        {}
        {/* Curriculum Checklist */}
        <section className="bg-white dark:bg-slate-800 rounded-3xl p-6 md:p-8 shadow-sm border border-slate-100 dark:border-slate-700/50">
          <h2 className="text-xl font-bold text-slate-800 dark:text-white mb-6 flex items-center gap-2">
            <BookOpen size={20} className={primaryText} /> โครงสร้างหลักสูตร (133 หน่วยกิต)
          </h2>
          
          <div className="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8">
            {/* GenEd */}
            <div className="space-y-4">
              <div className="flex justify-between items-center border-b border-slate-100 dark:border-slate-700 pb-2">
                <h3 className="font-semibold text-slate-800 dark:text-white">{CURRICULUM.genEd.title}</h3>
                <span className="text-sm font-bold text-slate-500 dark:text-slate-400">
                  {getProgress(['GE_TH','GE_EN','GE_SCI','GE_MATH','GE_SOC','GE_HUM','GE_ELEC'], 33).earned} / 33
                </span>
              </div>
              {Object.entries(CURRICULUM.genEd.sub).map(([key, val]) => {
                const prog = getProgress(key, val.req);
                return (
                  <div key={key} className="flex flex-col gap-1.5">
                    <div className="flex justify-between items-center text-sm">
                      <span className="text-slate-600 dark:text-slate-300 flex items-center gap-2">
                        {prog.complete ? <CheckCircle2 size={14} className="text-emerald-500" /> : <div className="w-3.5 h-3.5 rounded-full border border-slate-300 dark:border-slate-600" />}
                        {val.title}
                      </span>
                      <span className="text-slate-500 dark:text-slate-400 text-xs">{prog.earned}/{val.req}</span>
                    </div>
                    <div className="w-full bg-slate-100 dark:bg-slate-700 rounded-full h-1.5">
                      <div className={`h-1.5 rounded-full ${prog.complete ? 'bg-emerald-500' : primaryBg}`} style={{ width: `${Math.min(100, (prog.earned/val.req)*100)}%` }}></div>
                    </div>
                  </div>
                )
              })}
            </div>

            {/* Others */}
            <div className="space-y-6">
              {/* Core */}
              <div className="space-y-3">
                <div className="flex justify-between items-center border-b border-slate-100 dark:border-slate-700 pb-2">
                  <h3 className="font-semibold text-slate-800 dark:text-white">{CURRICULUM.core.title}</h3>
                  <span className="text-sm font-bold text-slate-500 dark:text-slate-400">
                    {getProgress('CORE', 27).earned} / 27
                  </span>
                </div>
                {(() => {
                  const prog = getProgress('CORE', 27);
                  return (
                    <div className="flex flex-col gap-1.5">
                      <div className="w-full bg-slate-100 dark:bg-slate-700 rounded-full h-1.5 mt-2">
                        <div className={`h-1.5 rounded-full ${prog.complete ? 'bg-emerald-500' : primaryBg}`} style={{ width: `${Math.min(100, (prog.earned/27)*100)}%` }}></div>
                      </div>
                    </div>
                  )
                })()}
              </div>

              {/* Specific */}
              <div className="space-y-4">
                <div className="flex justify-between items-center border-b border-slate-100 dark:border-slate-700 pb-2">
                  <h3 className="font-semibold text-slate-800 dark:text-white">{CURRICULUM.specific.title}</h3>
                  <span className="text-sm font-bold text-slate-500 dark:text-slate-400">
                    {getProgress(['SPEC_COMP', 'SPEC_ELEC'], 67).earned} / 67
                  </span>
                </div>
                {Object.entries(CURRICULUM.specific.sub).map(([key, val]) => {
                  const prog = getProgress(key, val.req);
                  return (
                    <div key={key} className="flex flex-col gap-1.5">
                      <div className="flex justify-between items-center text-sm">
                        <span className="text-slate-600 dark:text-slate-300 flex items-center gap-2">
                          {prog.complete ? <CheckCircle2 size={14} className="text-emerald-500" /> : <div className="w-3.5 h-3.5 rounded-full border border-slate-300 dark:border-slate-600" />}
                          {val.title}
                        </span>
                        <span className="text-slate-500 dark:text-slate-400 text-xs">{prog.earned}/{val.req}</span>
                      </div>
                      <div className="w-full bg-slate-100 dark:bg-slate-700 rounded-full h-1.5">
                        <div className={`h-1.5 rounded-full ${prog.complete ? 'bg-emerald-500' : primaryBg}`} style={{ width: `${Math.min(100, (prog.earned/val.req)*100)}%` }}></div>
                      </div>
                    </div>
                  )
                })}
              </div>

              {/* Free Elective */}
              <div className="space-y-3">
                <div className="flex justify-between items-center border-b border-slate-100 dark:border-slate-700 pb-2">
                  <h3 className="font-semibold text-slate-800 dark:text-white">{CURRICULUM.free.title}</h3>
                  <span className="text-sm font-bold text-slate-500 dark:text-slate-400">
                    {getProgress('FREE', 6).earned} / 6
                  </span>
                </div>
                {(() => {
                  const prog = getProgress('FREE', 6);
                  return (
                    <div className="flex flex-col gap-1.5">
                      <div className="w-full bg-slate-100 dark:bg-slate-700 rounded-full h-1.5 mt-2">
                        <div className={`h-1.5 rounded-full ${prog.complete ? 'bg-emerald-500' : primaryBg}`} style={{ width: `${Math.min(100, (prog.earned/6)*100)}%` }}></div>
                      </div>
                    </div>
                  )
                })()}
              </div>

            </div>
          </div>
        </section>

        {}
        {/* Course List & Management */}
        <section className="bg-white dark:bg-slate-800 rounded-3xl p-6 md:p-8 shadow-sm border border-slate-100 dark:border-slate-700/50">
          <div className="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
            <h2 className="text-xl font-bold text-slate-800 dark:text-white flex items-center gap-2">
              <BarChart size={20} className={primaryText} /> ผลการเรียนรายเทอม
            </h2>
            <button 
              onClick={() => setShowAddCourse(!showAddCourse)}
              className={`px-4 py-2 rounded-xl text-sm font-medium text-white ${primaryBg} hover:opacity-90 transition-opacity flex items-center gap-2 shadow-sm`}
            >
              {showAddCourse ? <X size={16} /> : <Plus size={16} />} 
              {showAddCourse ? 'ปิด' : 'เพิ่มรายวิชา'}
            </button>
          </div>

          {showAddCourse && (
            <CourseForm 
              onSave={(data) => {
                setCourses([...courses, data]);
                setShowAddCourse(false);
              }}
              onCancel={() => setShowAddCourse(false)}
            />
          )}

          <div className="space-y-6">
            {coursesByTerm.length === 0 ? (
              <div className="text-center py-10 text-slate-400 dark:text-slate-500">
                ยังไม่มีข้อมูลรายวิชา คลิก "เพิ่มรายวิชา" เพื่อเริ่มต้น
              </div>
            ) : (
              coursesByTerm.map(termGroup => (
                <div key={termGroup.term} className="border border-slate-100 dark:border-slate-700/80 rounded-2xl overflow-hidden bg-slate-50/50 dark:bg-slate-800/30">
                  <div className="px-5 py-4 border-b border-slate-100 dark:border-slate-700/80 flex flex-wrap justify-between items-center bg-slate-50 dark:bg-slate-800">
                    <h3 className="font-bold text-slate-800 dark:text-white">ภาคการศึกษา {termGroup.term}</h3>
                    <div className="flex gap-4 text-sm font-medium">
                      <span className="text-slate-500 dark:text-slate-400">หน่วยกิต: {termGroup.credits}</span>
                      <span className={primaryText}>GPA: {termGroup.gpa.toFixed(2)}</span>
                    </div>
                  </div>
                  <div className="overflow-x-auto">
                    <table className="w-full text-left text-sm whitespace-nowrap">
                      <thead className="bg-transparent text-slate-500 dark:text-slate-400">
                        <tr>
                          <th className="px-5 py-3 font-medium">รหัสวิชา</th>
                          <th className="px-5 py-3 font-medium">ชื่อวิชา</th>
                          <th className="px-5 py-3 font-medium text-center">หน่วยกิต</th>
                          <th className="px-5 py-3 font-medium text-center">เกรด</th>
                          <th className="px-5 py-3 font-medium text-right">จัดการ</th>
                        </tr>
                      </thead>
                      <tbody className="divide-y divide-slate-100 dark:divide-slate-700/50">
                        {termGroup.courses.map(course => (
                          <tr key={course.id} className="hover:bg-slate-50 dark:hover:bg-slate-700/30 transition-colors group">
                            <td className="px-5 py-3 text-slate-800 dark:text-slate-200 font-medium">{course.code}</td>
                            <td className="px-5 py-3 text-slate-600 dark:text-slate-300">
                              {course.name}
                              <span className="block text-xs text-slate-400 mt-0.5">
                                {CATEGORY_OPTIONS.find(c => c.value === course.category)?.label || course.category}
                              </span>
                            </td>
                            <td className="px-5 py-3 text-center text-slate-600 dark:text-slate-300">{course.credits}</td>
                            <td className="px-5 py-3 text-center">
                              <span className={`inline-flex items-center justify-center w-8 h-8 rounded-full font-bold text-xs ${course.grade === 'A' ? 'bg-emerald-100 text-emerald-700 dark:bg-emerald-500/20 dark:text-emerald-400' : course.grade === 'F' ? 'bg-rose-100 text-rose-700 dark:bg-rose-500/20 dark:text-rose-400' : 'bg-slate-100 text-slate-700 dark:bg-slate-700 dark:text-slate-300'}`}>
                                {course.grade}
                              </span>
                            </td>
                            <td className="px-5 py-3 text-right">
                              <button 
                                onClick={() => setCourses(courses.filter(c => c.id !== course.id))}
                                className="text-slate-400 hover:text-rose-500 transition-colors p-2 opacity-0 group-hover:opacity-100 focus:opacity-100"
                                title="ลบรายวิชา"
                              >
                                <Trash2 size={16} />
                              </button>
                            </td>
                          </tr>
                        ))}
                      </tbody>
                    </table>
                  </div>
                </div>
              ))
            )}
          </div>
        </section>

      </main>
      
      {/* Footer */}
      <footer className="text-center pb-8 pt-4 text-sm text-slate-400 dark:text-slate-500">
        GradWay — Minimal Degree Tracker Planner
      </footer>
    </div>
  );
}
